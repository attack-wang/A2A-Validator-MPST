# Host Platform

该目录包含验证框架的 Host 平台：

- `frontend/ui`：Mesop 用户界面及其 API 服务。
- `backend/python/hosts/multiagent`：多智能体 Host、A2A 连接和 Host 侧协议验证。
- `backend/python/extensions/timestamp`：时间戳扩展。

工作区只注册以上三个包。上游许可证和 Git 历史备份予以保留。
