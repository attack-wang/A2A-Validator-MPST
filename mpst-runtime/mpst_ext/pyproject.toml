[project]
name = "mpst_ext"
version = "0.2.0"
description = "MPST (Multiparty Session Types) validation extension for A2A agents"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["a2a-sdk>=0.3.0,<1.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
