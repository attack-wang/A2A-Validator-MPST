[project]
name = "mpst-a2a-validator"
version = "0.1.0"
description = "MPST-based protocol projection and runtime validation for A2A multi-agent communication."
requires-python = ">=3.13"
dependencies = ["mpst-ext"]

[tool.uv.sources]
mpst-ext = { path = "mpst-runtime/mpst_ext", editable = true }
